"""Built-in demo MicroPython client for the migration profile.

This example exists so the built-in demo profile resolves to a real client app
during the early profile/config phases before managed MicroPython deployment is
fully implemented.
"""


def main() -> None:
    """Minimal placeholder entrypoint for the demo profile."""

    print("demo profile")
